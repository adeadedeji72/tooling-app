pipeline {
    agent any

    stages{
        stage("build") {
            steps { 
                echo 'this is the build stage'

            }
        }

        stage("test") {
            steps { 
                echo 'this is the test stage'
                echo 'print this'

            }
        }
        stage("deploy") {
            steps { 
                echo 'this is the deployment stage'

            }
        }
    }
}